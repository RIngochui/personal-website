/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  noImplicitAny: true
}

module.exports = {
  nextConfig, 
  basePath: '/ringochui',
  assetPrefix: '/ringochui/',
}
